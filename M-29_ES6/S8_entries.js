const employee = {
    name: 'Raja Rani',
    designation: 'QA',
    salary: '20000',
    experience: 1
}

const keys = Object.keys(employee)  //for get object key or element
const values = Object.values(employee)  //for get object value
const entries = Object.entries(employee) //for get value and element both
console.log(keys)
console.log(values)
console.log(entries)